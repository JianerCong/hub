\documentclass{mypuzzle}
\title{My title}
\author{me}
\begin{document}

\maketitle

By default, in standard LaTeX classes the default style for text is usually a
Roman (upright) serif font. To use other styles (families) such as sans serif,
typewriter (monospace) etc. you need to use some specific LaTeX commands, as
shown in the example below:

\section{The Dimension Formula}
\begin{qst}
  Let $A$ be a $l \times m$ matrix and Let $B$ be an $n \times p$ matrix. Prove
  that the rule $M \to AMB$ defines a linear transformation from the space
  $F^{m \times n}$ of $m \times n$ matrices to the space $F^{l \times p}$.
\end{qst}
\begin{prf}
  The proof starts from the definition of linear transformation.
\end{prf}
\end{document}
